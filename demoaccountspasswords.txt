ScreenSense — Test Account Credentials

Sign in at the ScreenSense app (web or Expo Go).

  Email              Password
  user1@test.com     ScreenSense1!
  user2@test.com     ScreenSense2!
  user3@test.com     ScreenSense3!
  user4@test.com     ScreenSense4!


Notes
─────
- Passwords are hashed with PBKDF2-HMAC-SHA256 (100,000 iterations).
- Stored in: backend/data/screensense.db  (users table)
- To recreate test accounts (if DB was reset):
    cd backend && venv\Scripts\python ..\create_test_users.py
